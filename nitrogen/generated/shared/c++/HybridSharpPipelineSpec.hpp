///
/// HybridSharpPipelineSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ImageMetadata` to properly resolve imports.
namespace margelo::nitro::sharp { struct ImageMetadata; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include "ImageMetadata.hpp"

namespace margelo::nitro::sharp {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SharpPipeline`
   * Inherit this class to create instances of `HybridSharpPipelineSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSharpPipeline: public HybridSharpPipelineSpec {
   * public:
   *   HybridSharpPipeline(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSharpPipelineSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSharpPipelineSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSharpPipelineSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void resize(double width, double height, const std::string& fit) = 0;
      virtual void crop(double left, double top, double width, double height) = 0;
      virtual void rotate(double angle) = 0;
      virtual void blur(double sigma) = 0;
      virtual void sharpen(double sigma) = 0;
      virtual void jpeg(double quality) = 0;
      virtual void png(double compressionLevel) = 0;
      virtual void webp(double quality) = 0;
      virtual std::shared_ptr<Promise<std::string>> toFile(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> toBuffer() = 0;
      virtual std::shared_ptr<Promise<ImageMetadata>> metadata() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SharpPipeline";
  };

} // namespace margelo::nitro::sharp
